name: zongo-development

x-app-environment: &app-environment
  NODE_ENV: ${NODE_ENV:-production}
  # This alias exists only on this stack's private default network. The generic
  # `postgres` name can collide with another Coolify application on the shared
  # `coolify` network.
  POSTGRES_HOST: zongo-development-postgres
  POSTGRES_PORT: '5432'
  POSTGRES_USER: ${POSTGRES_USER:?POSTGRES_USER is required}
  POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
  POSTGRES_DB: ${POSTGRES_DB:?POSTGRES_DB is required}

x-infrastructure-depends-on: &infrastructure-depends-on
  postgres:
    condition: service_healthy
  redis:
    condition: service_healthy

x-app-depends-on: &app-depends-on
  <<: *infrastructure-depends-on
  migrate:
    condition: service_completed_successfully

x-application-build: &application-build
  context: .
  dockerfile: infra/docker/Dockerfile

x-app-service: &app-service
  build: *application-build
  restart: unless-stopped
  depends_on: *app-depends-on
  environment: *app-environment
  networks:
    - default
    - coolify

services:
  api:
    <<: *app-service
    image: zongo-development-api:latest
    command: ['node', 'dist/apps/api/main']
    environment:
      <<: *app-environment
      PORT: '3000'
    expose:
      - '3000'
    labels:
      - 'traefik.enable=true'
      - 'traefik.docker.network=coolify'
      - 'traefik.http.routers.zongo-dev-api.rule=Host(`${API_HOSTNAME}`)'
      - 'traefik.http.routers.zongo-dev-api.entrypoints=https'
      - 'traefik.http.routers.zongo-dev-api.tls=true'
      - 'traefik.http.services.zongo-dev-api.loadbalancer.server.port=3000'
    healthcheck:
      test:
        ['CMD-SHELL', 'curl -fsS http://localhost:3000/health/ready || exit 1']
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s

  worker:
    <<: *app-service
    image: zongo-development-worker:latest
    command: ['node', 'dist/apps/worker/main']
    environment:
      <<: *app-environment
      PORT: '3001'
    expose:
      - '3001'
    healthcheck:
      test:
        ['CMD-SHELL', 'curl -fsS http://localhost:3001/health/ready || exit 1']
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s

  admin:
    <<: *app-service
    image: zongo-development-admin:latest
    command: ['node', 'dist/apps/admin/apps/admin/src/main.js']
    environment:
      <<: *app-environment
      PORT: '3002'
      BREAK_GLASS_SECRET: ${BREAK_GLASS_SECRET:?BREAK_GLASS_SECRET is required}
      ADMINJS_COOKIE_SECRET: ${ADMINJS_COOKIE_SECRET:?ADMINJS_COOKIE_SECRET is required}
      ADMIN_ALERT_WEBHOOK_URL: ${ADMIN_ALERT_WEBHOOK_URL:-}
      # AdminJS defaults to a relative .adminjs directory; use a writable,
      # absolute container path because Coolify does not guarantee the CWD.
      ADMIN_JS_TMP_DIR: /tmp/adminjs
    expose:
      - '3002'
    labels:
      - 'traefik.enable=true'
      - 'traefik.docker.network=coolify'
      - 'traefik.http.routers.zongo-dev-admin.rule=Host(`${ADMIN_HOSTNAME}`)'
      - 'traefik.http.routers.zongo-dev-admin.entrypoints=https'
      - 'traefik.http.routers.zongo-dev-admin.tls=true'
      - 'traefik.http.routers.zongo-dev-admin.middlewares=zongo-admin-vpn@file'
      - 'traefik.http.services.zongo-dev-admin.loadbalancer.server.port=3002'
    healthcheck:
      test:
        ['CMD-SHELL', 'curl -fsS http://localhost:3002/health/ready || exit 1']
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s

  postgres:
    image: postgres:16.9-bookworm
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:?POSTGRES_USER is required}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      POSTGRES_DB: ${POSTGRES_DB:?POSTGRES_DB is required}
    volumes:
      - postgres-development-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB']
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    networks:
      default:
        aliases:
          - zongo-development-postgres

  redis:
    image: redis:7.4-bookworm
    restart: unless-stopped
    command:
      [
        'redis-server',
        '--appendonly',
        'yes',
        '--requirepass',
        '${REDIS_PASSWORD:?REDIS_PASSWORD is required}',
      ]
    volumes:
      - redis-development-data:/data
    healthcheck:
      test:
        [
          'CMD-SHELL',
          'redis-cli --no-auth-warning -a "$$REDIS_PASSWORD" ping | grep -q PONG',
        ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:?REDIS_PASSWORD is required}

  migrate:
    build: *application-build
    image: zongo-development-migrate:latest
    command: ['pnpm', 'exec', 'prisma', 'migrate', 'deploy']
    restart: 'no'
    depends_on: *infrastructure-depends-on
    environment: *app-environment
    # Coolify attaches one-off services to its resource network unless this is
    # explicit. PostgreSQL's private alias lives on the Compose default network.
    networks:
      - default

volumes:
  postgres-development-data:
  redis-development-data:

networks:
  coolify:
    external: true
